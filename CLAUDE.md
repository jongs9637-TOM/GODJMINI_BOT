# 이 프로젝트에 대해

이 폴더(`D:\automation-hub`)는 사용자(코딩 왕초보, "그밤PD 챌린지" 1기 참가 중)가 여러 개인 프로젝트를 함께 운영하는 작업 폴더입니다.

## 나는 어떤 사람인가

- **코딩을 전혀 모릅니다.** 코드를 보여줘도 이해 못 하니, 설명은 "무슨 파일을 어디에 저장했고 어떻게 여는지"처럼 행동 중심으로 짧게 해주세요.
- Claude Code(VS Code 통합)로 작업합니다. GitHub, Railway, Supabase, Telegram 대시보드 화면을 스크린샷으로 자주 보여주고, 어디를 눌러야 하는지 물어봅니다.
- git, 터미널, 배포 개념에 익숙하지 않습니다. `git init`, `push`, `Secrets 등록`, `Actions 탭` 같은 절차는 매번 "정확히 어디를 클릭"까지 단계별로 알려줘야 합니다.

## 내가 좋아하는 방식

- **직접 파일을 만들고 저장한 뒤, 저장 위치와 여는 법을 알려주는 것.** "코드는 이렇습니다" 만 던지지 말고 실행까지 확인해주세요.
- 실행/배포 전에 **먼저 순서(계획)를 짧게 정리**해서 확인받는 걸 좋아합니다 ("만들기 전에 순서부터 알려줘").
- 에러가 나면 **화면을 통째로 캡처해서 보여주는 것**으로 소통합니다 — 요약하지 않고 그대로 보여주면 원인을 바로 찾아주세요.
- 제3자 코드(다운로드한 zip, 외부 키트 등)를 실행하기 전에는 안전한지 검토하고 알려주는 걸 신뢰합니다.
- 규모가 큰 새 작업(새 배포 대상, 새 리스크가 있는 자동화)은 바로 진행하지 말고 옵션을 제시해서 고르게 해주세요.

## 프로젝트 구조

- **`packages/`** — Threads 자동화 봇 (TypeScript, Node.js). Railway에 배포됨 (public 저장소지만 `.env`는 gitignore). Playwright 세션 기반으로 Threads에 실제 포스팅. 대시보드는 `packages/bot/src/dashboard/`, Basic Auth로 보호(`DASHBOARD_USER`/`DASHBOARD_PASSWORD`).
- **`auto/`** — 코딩 왕초보용 개인 도구 모음 (점심 뽑기, 디데이 카운터, N빵 계산기 등). 각 도구는 단일 HTML 파일, 설치 불필요, localStorage 저장 + 저장 실패 시 안전한 폴백. GitHub Pages(Actions 방식)로 자동 배포됨 → `https://jongs9637-tom.github.io/GODJMINI_BOT/`. 새 도구 추가 시 `auto/index.html`(도구함 대문)에도 카드 추가할 것.
- **`market-kit/`** — 시황 키트(제3자 제공, 부트캠프 자료). **이 폴더는 별도의 git 저장소**이며 `automation-hub`의 git에는 포함되지 않음(gitignore 처리). 실제 코드는 비공개 저장소 `jongs9637-TOM/sihwang-kit`에 따로 push되어 있고, GitHub Actions로 평일 아침 자동 실행됨.

## 알아둘 것

- Railway는 이 저장소(`GODJMINI_BOT`)를 감시하는데, **Watch Paths가 `packages/**`로 제한되어 있어** `auto/`나 다른 폴더만 바꿔도 Railway가 불필요하게 재배포하지 않습니다. 새 폴더를 추가해도 이 설정은 그대로 유지하면 됩니다.
- 텔레그램 봇은 `GODJMINI_BOT` 하나를 여러 용도(Threads 봇 알림 + 시황 키트)로 같이 씁니다. 토큰/챗ID는 이미 확보되어 있음 — 필요하면 기존 대화에서 재사용.
- git 커밋할 때 `git add -A`/`git add .` 대신 파일을 명시해서 스테이징하는 걸 선호합니다 (자동 승인 정책과도 맞음).
- 새 정적 도구를 만들 때는 이 저장소에서 써온 디자인(크림/오렌지 톤, 큰 글씨, 모바일 우선, 저장 실패 시 경고 배너)을 따라가면 일관성이 유지됩니다.
